{
  "tabs": [
    {
      "id": "general",
      "label": "General",
      "fields": [
        {
          "key": "debug_mode",
          "label": "Debug mode",
          "component": "checkbox",
          "description": "Enable verbose debug logging in the admin UI. Disable in production (live mode)."
        },
        {
          "key": "default_per_page",
          "label": "Rows per page",
          "component": "input",
          "inputType": "number",
          "min": 10,
          "max": 200,
          "description": "Default page size on the moderation tables (nicknames, transfers)."
        },
        {
          "key": "show_system_messages",
          "label": "Highlight system messages",
          "component": "checkbox",
          "description": "Visually distinguish token-transfer system rows in the conversation inspector."
        }
      ]
    }
  ]
}
